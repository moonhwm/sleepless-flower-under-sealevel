# 全量自主审计报告（v68.78，autonomous-advance-protocol）

> 审计时间 2026-08-28 · 模式：K3 长对话单线程（全栈单线程） · 审计范围：主线评分系统全量

## 审计六维结果

| 维度 | 结论 | 级别 |
|---|---|---|
| ① 完整性 | ruanke 台账本地补全（原仅 GitHub）；其余数据/模块/测试/文档齐 | **Med（已修）** |
| ② 一致性 | VERSION=v68.78=__version__=git 一致；param=231 缺解释=0；__all__=131 | 无缺陷 |
| ③ 正确性 | 关键结论证据链抽查 4/4 过（南昌坍缩-100/北科大 0-74/浙工大 α/山西 070205 真空） | 无缺陷 |
| ④ 安全性 | 敏感词仅在 test_engine.py 隐私断言名单（防泄露机制，非泄露）；开源仓库已脱敏零命中；本地明文=隐私测试必需，合规 | 无缺陷 |
| ⑤ 冗余性 | data/ 台账无重复；0KB 文件实为误报（实际 714-926B 正常） | 无缺陷 |
| ⑥ 边界诚实 | 待核项标注齐（北科大 confB/37 校 pv 未定值/未上榜 38 校 confC） | 无缺陷 |

## 总判定

**1 个 Med 级缺陷（ruanke 台账本地缺失）已修复；其余无缺陷。**

## 修复记录

- `data/ruanke_2025_physics.json` 本地补全（原仅在 GitHub 推送，本地工作区缺失）——已落盘。

## 审计纪律

- 所有判定基于 ls/读文件/grep 的磁盘核验（非记忆）；
- 敏感词命中判定为"隐私断言名单"（test_engine.py 里 `assert name not in raw` 的防泄露机制），非泄露本身——本地工作区合规，开源仓库（GitHub）已脱敏零命中；
- 数据缺口/待核项全部如实标注（conf 降级）。

---

**审计方**：Kimi（K3 长对话） · **核验**：全部判定基于磁盘核验 · **回退起点**：v68.78（git e5bd60d）
