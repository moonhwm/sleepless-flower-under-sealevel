# 交接文档：物理硕士择校评分系统 v68.69（七技能全跑核查后）

> 生成 2026-08-27 / data_cutoff=2026-08-27 / 给下一个 K3/K2.6 会话
> 交接五件套之①交接文档；配套：②可信数据集=项目 data/*.json；③可运行引擎=scoring_system 包；④状态看板=status_board.json；⑤下会话首任务卡=下方末节

## 一、硬结论（下会话不得矛盾，矛盾就改旧文件）

1. **无黑名单机制**（confA，代码可验）：深大/南昌从未进黑名单；"逃离光学重载"仅南昌 notes 用户个人适配文本，引擎 photo_couple/semi 是加分维（权重 3.0/8.0）。深大物理 28.26（双非最高梯队）/南昌 24.63。
2. **三链分离**（confA，Wind 实证）：光伏（隆基链）≠锂电（宁德链）≠LED（南昌硅基 LED=半导体照明/深大光电=微纳光电子）。LED 不在光伏/锂电链，不加分。
3. **链级 ETF 修正单龙头**（v68.69 拆链）：光伏 ETF515790 较低点+35%/2026 均 1.020>2024 均 0.779=板块修复（隆基-47%是个体价格战，单龙头以偏概全已断裂）→pv=1.5；锂电 ETF159755 较低点+101%链级确认→lithium=2.0。
4. **0702 降温=confA 硬结论**（v68.69 无污染验证）：β 校（零调剂）纯一志愿地板 score_min，13 校中 10 校降温（南昌 374→278=-96/南师大-69/哈工大-68）仅云大+29。非β校 18 所"坍缩"口径存疑（必达分含调剂）。
5. **53 校 59 代码台账**：panel_pdf_ledger(53 校)/panel_critique(三模式+坍缩+异常)/panel_code_scan(代码级)。北科大 2026 一志愿 74 复试 0 录取（confB 待 9 月官方名单复核）。南昌=β堡垒+必达 383→283(-100)最大坍缩，但 639 曾谨言 D_q90+光学重载→观察名单不作主航道。
6. **捡漏组合 v2**：B 区主力=昆明理工(α/双代码 275)/宁夏大学(α)/海大(真空)/西北师大(真空)；A 区=山西 070205 凝聚态(真空 285)/浙工大/杭电。剔除烟大/温大（γ 嫌疑通过率缺测）。
7. **Luna 趋近范式**（v68.69 新增，用户画像）：花未眠（凌晨四点独自凝视）+ Luna（向月亮移动的等离子体）= 元叙事。五条防误读锁见 docs/用户画像_Luna趋近范式.md。**元叙事与数据严格分层**——Luna 不得作为任何院校可考性的论据。

## 二、环境脆弱性（沙箱重置必丢，下会话先修）

- pytest 丢失→`pip install -q pytest`；git 丢失→`git config --global --add safe.directory <项目路径>`+`git init`+`git config user.*`+重建 `.git/hooks/pre-commit`（test_v68_39 断言 hook 存在）。
- 权限：shell=root(uid0)，IPython=kimi(uid999)；shell 写文件后必 `chown -R 999:999 项目目录`。
- 长测试 570 项约 25-30 分钟，用后台 `> /tmp/x.log 2>&1` + sleep 轮询，禁止调用里 sleep 等待（504）。

## 三、评分引擎口径（防误改）

- `physics_scale = physics_target / sum(w.values())`——新增权重**禁止**入主表 w（会摊薄全库标尺，v68.68 已修一次）；方向子维度走 `cfg["pv_lithium_weights"]` 独立通道。
- param_registry 220 项，audit() 缺解释=0 为硬门槛；新增参数必须带 rationale+来源+conf+调整。
- 测试断言锚：__version__='68.69'，__all__=124，param=220，schools=113，台账校=53，代码=59。

## 四、每日凌晨四点自检机制（呼应"花未眠"）

用户要求：每日凌晨四点定时自检监听任何核心成果及证据链文件，确保落库 /output。

**实现路径（诚实说明）**：本会话沙箱无常驻进程能力，需用定时任务工具（add_cron_job）注册每日 04:00 自检——检查项：①data/*.json 关键台账存在性；②pytest 全绿；③git 状态干净；④交接文档与胶囊时效。核心成果与证据链已全部落库 /mnt/agents/output/ 与 /mnt/agents/upload/（双保险）。

## 五、下会话首任务卡（2026-09 简章季，按序）

1. `ls` 核验关键文件（见 status_board.json threads[0].key_files）。
2. 7 花名册校（昆明理工/兰州理工/燕山/上海理工/华东理工/南京师大/北科大）官网查初试科目→补画像→评估入库（113→120）。
3. 北科大 0/74 官方名单复核→定性真零录取还是批次不全。
4. 南昌 639 曾谨言书目复核→定去留（观察名单↔冲高候选）。
5. 坍缩校 2027 报名数据重估（防均值回归）。

## 六、挂账清单

- **视频生成**：agent-gw 视频接口 2026-08-27 持续 RemoteDisconnected（4 次尝试失败），封面动态视频（海棠花+月+海面）未产出。接口恢复后补，网页封面暂用静态图。
- **网盘/云文档审计**：baidu-pan 与 kdocs 需用户自持 access_token/授权，当前环境无凭据，**未审计**。若需，用 doc-archive-ingest 技能的分享链接通道，或提供 kdocs 授权。
- **9 月简章季**：7 花名册校补画像入库；北科大 0/74 复核；南昌 639 复核；坍缩校报名数据重估。
- **2027-03 复试名单季**：γ 校 6 所通过率补证；山西矛盾数据复核。
- **季度复拉**（2026-11）：光伏 ETF515790/锂电 ETF159755/LED 龙头三安光电→pv/lithium 权重调整。
- **37 校 pv/lithium 未定值**：桶 A 补证后或下调。

## 七、K3 协助点（桶分派）

- 桶 A 集群：37 校光电/半导体 pv_lithium 定值；γ 校 6 所通过率补证；7 花名册校画像。
- 桶 B 长对话：南昌 639 真题逐题适配；坍缩均值回归建模；北科大 0/74 专项；钙钛矿产业化专项。
- 桶 C K2.6：9 月简章周监测；台账快查答问；季度复拉 ETF。

## top3_likely_wrong
1. 37 校 pv/lithium 未定值，5 校初值是 notes+单龙头推的，桶 A 补证后可能下调。
2. LED 链"中性"缺数据（未拉三安光电），南昌 0 分或偏低。
3. γ 校 6 所（兰州理工/南理工/浙江科技/烟大/温大/北科大）通过率缺测，若实际不刷人应从 γ 移出。
